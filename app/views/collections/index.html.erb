<% content_for :sub_title, "Hand picked Github projects" %>
<% if user_signed_in? %>
  <% content_for :action do %>
    <% link_to 'Create a new collection &nbsp; <i class="fa fa-plus" aria-hidden="true"></i>'.html_safe, new_collection_path, class: "button is-primary" %>
  <% end %>
<% end %>
<%= render @collections %>
<%= paginate @collections %>

<%= render 'modal' %>
